import { AddToCartHighlight } from './add-to-cart-highlight';

describe('AddToCartHighlight', () => {
  it('should create an instance', () => {
    const directive = new AddToCartHighlight();
    expect(directive).toBeTruthy();
  });
});
